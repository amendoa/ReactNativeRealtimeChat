import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
	default: {
		height: 50,
		backgroundColor: '#fff',
		borderColor: '#ffffff',
		borderWidth: 1,
		borderRadius: 4,
		margin: 2
	}
});