import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
	default: {
		height: 50,
		backgroundColor: '#fff',
		borderColor: '#ffffff',
		borderWidth: 0.5,
		borderRadius: 4,
		margin: 2
	},
	error: {
		borderColor: '#fe0000',
	}
});